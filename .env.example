# Govee MCP Server Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# REQUIRED
# =============================================================================

# Your Govee API key
# Get one at: https://developer.govee.com/
GOVEE_API_KEY=your-govee-api-key-here

# Comma-separated list of valid client tokens for authentication
# Clients must include one of these tokens in the x-mcp-auth header
# Generate secure random tokens, e.g.: openssl rand -hex 32
MCP_CLIENT_TOKENS=token1,token2

# =============================================================================
# SERVER (Optional)
# =============================================================================

# Server port (default: 3000)
# PORT=3000

# Server host (default: 0.0.0.0)
# HOST=0.0.0.0

# Environment: development, production, or test (default: development)
# NODE_ENV=production

# =============================================================================
# LOGGING (Optional)
# =============================================================================

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=info

# =============================================================================
# CACHING (Optional)
# =============================================================================

# Device cache TTL in milliseconds (default: 300000 = 5 minutes)
# DEVICE_CACHE_TTL_MS=300000

# =============================================================================
# RATE LIMITING (Optional)
# =============================================================================

# Maximum requests per client per rate limit window (default: 60)
# PER_CLIENT_RATE_LIMIT=60

# Rate limit window in milliseconds (default: 60000 = 1 minute)
# RATE_LIMIT_WINDOW_MS=60000

# =============================================================================
# RETRY CONFIGURATION (Optional)
# =============================================================================

# Maximum retry attempts for Govee API calls (default: 3)
# MAX_RETRIES=3

# Initial retry backoff in milliseconds (default: 1000)
# INITIAL_BACKOFF_MS=1000

# Maximum retry backoff in milliseconds (default: 10000)
# MAX_BACKOFF_MS=10000

# =============================================================================
# COMMAND COALESCING (Optional)
# =============================================================================

# Window for batching rapid commands in milliseconds (default: 200)
# Helps prevent API flooding when multiple commands are sent quickly
# COALESCE_WINDOW_MS=200

# =============================================================================
# DEVICE DEFAULTS (Optional)
# =============================================================================

# Default device ID for commands without explicit target
# Format: MAC address (e.g., AA:BB:CC:DD:EE:FF)
# DEFAULT_DEVICE_ID=
